package pl.sumatywny.travelmate.participant.model;

/**
 * Defines the possible roles a user can have within a trip.
 * Different roles have different permissions and capabilities.
 */
public enum ParticipantRole {
    ORGANIZER,
    MEMBER,
    GUEST
}